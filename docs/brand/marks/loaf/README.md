# The loaf (2026-09-06)

**Decided: loaf, rounded top** (`loaf-round.svg`), hole-highlight eyes (founder: the inverted glance on dark
is fine; `eyes.html` shows the alternatives). `ears.py` + `ears.html`: the ear roundness ladder for the
final tweak. `web/public/favicon.svg` carries the chosen file; `make brand` regenerates every raster and
the README's two flat marks (brand.mjs flattens the favicon's two schemes properly, tail stroke included).
