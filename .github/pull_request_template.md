<!-- Thanks. Small fixes need no issue; anything larger, please link the issue or discussion where it was agreed (CONTRIBUTING.md). -->

**What this changes, in one paragraph**



**How you checked it**

- [ ] `make check`
- [ ] `cd web && pnpm typecheck && pnpm test && pnpm lint` (if `web/` changed)
- [ ] `make notices-check` (if a dependency changed; `make notices` regenerates the file)
- [ ] For a copy change: the sentence is true of the code as it is

**Anything a reviewer might mistake for a bug that is intended**

